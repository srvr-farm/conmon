defaults:
  interval: 30s
  timeout: 5s

groups:
  - name: external
    checks:
      - id: bad-record
        name: Bad Record Type
        kind: dns
        scope: external
        query_name: example.com
        record_type: WAT

export:
  listen_address: 0.0.0.0:9109
