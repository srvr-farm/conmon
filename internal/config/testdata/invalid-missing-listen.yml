defaults:
  interval: 30s
  timeout: 5s

groups:
  - name: internet
    checks:
      - id: internet-gateway
        name: Gateway
        kind: icmp
        scope: internet
        host: 1.1.1.1

export: {}
